🚗 Car Price Prediction

This project uses Machine Learning to predict the selling price of used cars based on features like fuel type, transmission, and kilometers driven.

A Random Forest Regressor is used to train the model after preprocessing the dataset. The model is then used to predict car prices and visualize the results using a scatter plot of actual vs predicted values.

Technologies: Python, Pandas, Scikit-learn, Matplotlib
